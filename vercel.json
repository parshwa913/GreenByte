{
  "buildCommand": "npm run build",
  "outputDirectory": "web-build",
  "routes": [
    { "src": "/admin(.*)", "dest": "/index.html" },
    { "src": "/customer(.*)", "dest": "/index.html" },
    { "src": "/recycler(.*)", "dest": "/index.html" },
    { "src": "/*", "dest": "/index.html" }
  ]
}
